apiVersion: v1
kind: Service
metadata:
  name: saas-starter-api
  namespace: saas-starter
  labels:
    app: saas-starter-api
spec:
  type: ClusterIP
  selector:
    app: saas-starter-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
